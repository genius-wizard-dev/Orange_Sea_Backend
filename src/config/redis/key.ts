export const USER_DEVICE_INFO = (profileId: string, deviceId: string) =>
  `user:${profileId}:device:${deviceId}`;
export const SOCKET_TO_ACTIVE_GROUP = 'socket:group:';
export const SOCKET_MAP_DATA = 'socket:';
export const PROFILE_CONNECTIONS = 'socket:profile:';
export const SOCKET_EXPIRATION = 86400;
